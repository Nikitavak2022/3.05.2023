#include <iostream>
using namespace std;
int ** creat(int &n, int &m)
{
        cout << "n = ";
        cin >> n;
        cout << "m = ";
        cin >> m;
        int **mas - new int *[n];
        for (int i = 0; i < n; ++i)
        mas[i] = new int [2 * m]
}
